info:
  app:
    name: catalog-playlist-service
    description: playlist

endpoints:
  restart:
    enabled: true
  refresh:
    enabled: true

server:
  port: 8106

spring:

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

playlist:
  streamService: http://localhost:80

---
spring:
    profiles: docker

server:
  port: 8080

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/